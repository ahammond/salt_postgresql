UserParameter=pgbouncer.requests_elite,sudo -u postgres psql -h 127.0.0.1 --port {% if pgbouncer_config.listen_port is defined %}{{ pgbouncer_config.listen_port }}{% else %}5432{% endif %} --dbname pgbouncer --quiet --no-align --tuples-only --command "SHOW STATS" 2>/dev/null | perl -F"\|" -lane "print @F[1] if @F[0] eq 'elite'";
